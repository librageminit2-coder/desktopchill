# ============================================================
#  Video GỐC nặng — GIỮ Ở MÁY, KHÔNG đưa lên web/GitHub.
#  Từ nay video mẫu để trong thư mục Library/ (bản gốc nặng).
#  Chỉ bản preview nhẹ trong media/preview mới được đăng lên web.
# ============================================================
/Library/*.mp4
/Library/*.MOV
/Library/*.mov
/*.mp4
/*.MOV
/*.mov

# Ảnh feedback GỐC (nặng) — bản tối ưu cho web nằm ở assets/img/feedback/
/feedback/

# Ghi chú / ảnh tham khảo nội bộ (không cần công khai)
/Reference information.txt
/reference website.txt
/landing page reference.png
/web on phone.jpg
/paper-texture.jpg

# Thư mục chứa file gốc / tham khảo — KHÔNG đưa lên web
/reference/

# Rác hệ điều hành / editor
Thumbs.db
.DS_Store
node_modules/
